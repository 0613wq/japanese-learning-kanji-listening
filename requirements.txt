# ═══════════════════════════════════════════════════════════════
# 段错误(Segmentation fault)的根因就在这里：
#
# 你原来的 requirements.txt 只锁了 streamlit==1.39.0，其余依赖不锁版本。
# uv 于是装上了 2026 年最新的 pyarrow==25.0.0 —— 而 streamlit 1.39
# 是 2024 年 10 月的版本，和 pyarrow>=19 的二进制不兼容（官方已知问题，
# streamlit 1.42 才修复），进程一启动就 Segmentation fault，
# 所以你在网页上做任何操作都会报错/断连。
#
# 修复方案：升级 streamlit 到新版（兼容新 pyarrow），并锁住关键依赖。
# ═══════════════════════════════════════════════════════════════
streamlit>=1.45,<2.0
pyarrow>=14,<20
pandas>=2.2,<2.3
numpy>=1.26,<2.0
edge-tts>=6.1.9,<7.0
requests>=2.32
