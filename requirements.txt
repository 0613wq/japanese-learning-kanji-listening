# ═══════════════════════════════════════════════════════════════
# 原始段错误根因：老版 streamlit==1.39.0 + 未锁版本被装上的最新
# pyarrow==25，二进制不兼容，进程启动即崩。
#
# 修复思路：只升级 streamlit 到新版本即可 —— 新版 streamlit 自带
# 对新版 pyarrow / numpy 的兼容约束，不要再手动给它们钉上限，
# 否则会和 streamlit 的依赖解析冲突，导致 pip 回退去编译
# pyarrow/pandas 源码包（部署卡死几十分钟）。
# ═══════════════════════════════════════════════════════════════
streamlit>=1.45,<2.0
edge-tts>=6.1.9,<7.0
pandas>=2.2,<3.0
requests>=2.32
