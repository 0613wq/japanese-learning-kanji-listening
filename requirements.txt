# ═══════════════════════════════════════════════════════════════
# v5.2 依赖说明：
#
# · streamlit：升级到新版修复了原始段错误（老版 1.39 与新 pyarrow
#   二进制不兼容）。不要给 pyarrow/numpy 手动钉上限，交给 streamlit
#   自己解析，否则会逼 pip 编译源码包、部署卡死。
#
# · edge-tts 必须 >=7.0！！微软语音接口在 2024 年底加了 token 校验，
#   6.x 版本会被直接拒绝（401/403），表现为"音频生成全部失败"。
# ═══════════════════════════════════════════════════════════════
streamlit>=1.47,<2.0
edge-tts>=7.0,<8.0
pandas>=2.2,<3.0
requests>=2.32
